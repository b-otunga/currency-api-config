apiVersion: v1
kind: Service
metadata:
  name: currency-api-stable
spec:
  selector:
    app: currency-api
  ports:
  - port: 8000
    targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: currency-api-canary
spec:
  selector:
    app: currency-api
  ports:
  - port: 8000
    targetPort: 8000
